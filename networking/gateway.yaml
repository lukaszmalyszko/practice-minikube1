apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: botapp-gateway
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: botapp
spec:
  hosts:
  - "*"
  gateways:
  - botapp-gateway
  http:
  - match:
    - uri:
        exact: /
    route:
    - destination:
        host: server
        port:
          number: 8080