apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: bot-v3
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: bot
        version: v3
    spec:
      containers:
      - name: bot
        image: bot:v3
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
